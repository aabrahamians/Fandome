
    <%= bootstrap_form_for [@tvshow, @character] do |f| %>
       
        <%= f.text_field :name %> <br>
        <%= f.text_area :description %>
        
        <%= f.submit "Add character!" %>

    <% end %>

