 <%= bootstrap_form_for [@tvshow, @character, @story] do |f| %>
       
		<div class="container-fluid">
<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= f.text_field :title %> <br>
        <%= f.text_area :body %>
		
        
        <%= f.submit "Add show!" %>
		
    <% end %>

</div></div></div>